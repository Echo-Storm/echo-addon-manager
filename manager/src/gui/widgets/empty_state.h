#pragma once

namespace eam {
namespace widgets {

// A centred placeholder for an empty list: a vector icon, a title and a wrapped hint. `width` is the width of the area it sits in.
void EmptyState(float width, const char* title, const char* hint);

} // namespace widgets
} // namespace eam
