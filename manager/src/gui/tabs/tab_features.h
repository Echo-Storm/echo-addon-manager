#pragma once

namespace lsproxy {

// The manager's built-in features (ReShade input passthrough, Windowed mode): a card each with its switch and, when it is on, its options.
void RenderTabFeatures();

} // namespace lsproxy
